# Multi-NPC experimental update 2026-09-24

User confirmed previous single-NPC automatic spawning, consistent appearance/actions and synchronized health. AI was passive. New local build expands a frozen plan to 1–4 same-template NPCs, IDs 100–103, selected from M_NPC_EGUN / M_NPC_GAODAO / M_NPC_BEAR. Shared base position and 20-unit X spacing guarantee identical initial placements on both replicas. NPC3 retains 192-byte layout and consumes offsets 52/56/60 for count/template/AI. Request 21901 is now 79 bytes; acknowledgement 21900 is 63 bytes and attests the same triple. Legacy 51-byte ack remains valid only for the legacy single EGUN/pve008 plan. No ready acknowledgement after partial native factory failure.

AI option 0 retains pve008, option 1 uses original pve006. Original script inspection shows activation radii 150 vs 200; pve008 melee random branches end at 14, pve006 at 30 with additional skill/jump choices. This is a static rationale, not proof of realized attack frequency. Every NPC AI must load before any starts; all replicas must already acknowledge. Existing emitter flags guard against repeated AI initialization. Only the owner controls all admitted NPC IDs.

Win32 UI includes 3 embedded original-model BMP thumbnails, count selector and AI selector. render_previews.py renders original DFF geometry/materials using the current GM decodeTexture implementation verbatim at build time, not a new decoder. Runtime EXE requires no Python or external images. DFF files cha_05_014 and cha_05_004 both reference cha_05_014 texture and produce the same static preview in this installed resource set; thumbnails were visually inspected. Bear preview distinct. Labels come from npcmanager.xml (恶棍 / 大刀男 / 熊).

Validation: go test ./internal/game ./cmd/server passed; C++ build.cmd passed without warnings; self-test and UI self-test passed, including malformed NPC3 options and embedded images. Server tests cover admitted ID range and owner isolation, invalid counts/templates/AI, plan data, failures/timeouts. New multiple-NPC behavior and aggressive AI not executed in game by the agent; user acceptance pending.

User authorized replacement/restart. Local server deployed at server/dist/local-server/kungfu-server.exe SHA256 69584A762B0367CA56F58739EB27C0FB2C7DDEA04D0A620D7BA17EDF4F9A36B7, PID 44168 at deployment, health OK. Tool SHA256 F4123A2E684E4169950B0233A9B2273D2C6CA62594833B144EAA5F79534601D4. Backup under runtime-local/team-mod-multi-20260924. No production or schema changes.
